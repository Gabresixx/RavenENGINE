import{describe,expect,it}from'vitest';import{MaterialCompiler}from'../packages/materials/src/index';describe('material compiler',()=>{it('compiles scalar graph expressions',()=>{const c=new MaterialCompiler().compile({name:'wet concrete',nodes:[{id:'n',kind:'noise',inputs:{scale:2}},{id:'r',kind:'multiply',inputs:{a:'n',b:.7}}],outputs:{roughness:'r'}});expect(c.outputExpressions.roughness).toContain('*');});});
