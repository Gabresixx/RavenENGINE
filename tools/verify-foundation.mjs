import{readdir,readFile,stat}from'node:fs/promises';import{join,relative,sep}from'node:path';const root=process.cwd(),packagesDir=join(root,'packages');const failures=[];const files=[];async function walk(dir){for(const name of await readdir(dir)){const path=join(dir,name),s=await stat(path);if(s.isDirectory())await walk(path);else if(/\.(ts|tsx|js|mjs)$/.test(name))files.push(path);}}await walk(packagesDir);const lowLayerRules={platform:new Set(),math:new Set(),serialization:new Set(['platform']),core:new Set(['platform','serialization'])};const domain=/\b(TLOU|RDR2|Seattle|infected|survivor|The Last of Us|Red Dead Redemption)\b/i;for(const path of files){const rel=relative(root,path).split(sep).join('/'),text=await readFile(path,'utf8'),pkg=rel.split('/')[1];if(domain.test(text))failures.push(`${rel}: game-specific domain term leaked into engine package`);if(/\bMath\.random\s*\(/.test(text))failures.push(`${rel}: Math.random is forbidden; use deterministic RNG`);if(pkg==='core'&&/\b(requestAnimationFrame|cancelAnimationFrame|indexedDB|localStorage|sessionStorage|navigator|new\s+Worker)\b/.test(text))failures.push(`${rel}: browser global leaked into core`);const allowed=lowLayerRules[pkg];if(allowed){for(const match of text.matchAll(/from\s*['"]@raven\/([^'"]+)['"]/g)){const target=match[1];if(!allowed.has(target))failures.push(`${rel}: low-layer package ${pkg} cannot import @raven/${target}`);}}}const required=['packages/platform/src/Platform.ts','packages/serialization/src/Snapshot.ts','packages/core/src/ModuleGraph.ts','packages/core/src/HandlePool.ts','packages/core/src/ResourceManager.ts','packages/core/src/Cancellation.ts','packages/core/src/Backend.ts','docs/ENGINE_FOUNDATION_CHECKLIST.md'];for(const rel of required)try{await stat(join(root,rel));}catch{failures.push(`${rel}: required foundation artifact missing`);}if(failures.length){console.error('RAVEN foundation verification failed:\n'+failures.map(x=>` - ${x}`).join('\n'));process.exit(1);}console.log(`RAVEN foundation invariants OK (${files.length} source files scanned).`);
