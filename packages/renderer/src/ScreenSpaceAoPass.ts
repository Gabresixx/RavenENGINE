import{FullscreenPass}from'./FullscreenPass';
const fs=`#version 300 es\nprecision highp float;in vec2 vUv;out vec4 outColor;uniform sampler2D uDepth;uniform vec2 uInvResolution;uniform float uRadius;float depthAt(vec2 uv){return texture(uDepth,uv).r;}void main(){float c=depthAt(vUv);if(c>=.99999){outColor=vec4(1);return;}vec2 dirs[8]=vec2[8](vec2(1,0),vec2(-1,0),vec2(0,1),vec2(0,-1),vec2(.707,.707),vec2(-.707,.707),vec2(.707,-.707),vec2(-.707,-.707));float occ=0.;for(int i=0;i<8;i++){float d=depthAt(vUv+dirs[i]*uInvResolution*uRadius);float delta=c-d;occ+=smoothstep(.00015,.0045,delta);}float ao=clamp(1.-occ/8.*.72,.28,1.);outColor=vec4(ao,ao,ao,1);}`;
export class ScreenSpaceAoPass extends FullscreenPass{texture!:WebGLTexture;framebuffer!:WebGLFramebuffer;width=0;height=0;constructor(gl:WebGL2RenderingContext){super(gl,fs);}ensure(width:number,height:number):void{if(this.width===width&&this.height===height)return;this.releaseTargets();this.width=width;this.height=height;const g=this.gl,t=g.createTexture(),f=g.createFramebuffer();if(!t||!f)throw new Error('Failed to allocate AO target');this.texture=t;this.framebuffer=f;g.bindTexture(g.TEXTURE_2D,t);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_MIN_FILTER,g.LINEAR);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_MAG_FILTER,g.LINEAR);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_WRAP_S,g.CLAMP_TO_EDGE);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_WRAP_T,g.CLAMP_TO_EDGE);g.texImage2D(g.TEXTURE_2D,0,g.R8,width,height,0,g.RED,g.UNSIGNED_BYTE,null);g.bindFramebuffer(g.FRAMEBUFFER,f);g.framebufferTexture2D(g.FRAMEBUFFER,g.COLOR_ATTACHMENT0,g.TEXTURE_2D,t,0);g.bindFramebuffer(g.FRAMEBUFFER,null);}render(depth:WebGLTexture,sourceWidth:number,sourceHeight:number,radius=3):void{const g=this.gl;g.bindFramebuffer(g.FRAMEBUFFER,this.framebuffer);g.viewport(0,0,this.width,this.height);g.disable(g.DEPTH_TEST);this.program.use();g.activeTexture(g.TEXTURE0);g.bindTexture(g.TEXTURE_2D,depth);g.uniform1i(this.program.uniform('uDepth'),0);g.uniform2f(this.program.uniform('uInvResolution'),1/sourceWidth,1/sourceHeight);g.uniform1f(this.program.uniform('uRadius'),radius);this.draw();g.enable(g.DEPTH_TEST);g.bindFramebuffer(g.FRAMEBUFFER,null);}private releaseTargets():void{if(this.texture)this.gl.deleteTexture(this.texture);if(this.framebuffer)this.gl.deleteFramebuffer(this.framebuffer);this.width=this.height=0;}override dispose():void{this.releaseTargets();super.dispose();}}
