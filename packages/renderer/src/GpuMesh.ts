export interface CpuMeshLike{positions:Float32Array;normals:Float32Array;uvs?:Float32Array;indices:Uint16Array|Uint32Array;}
export class GpuMesh{readonly vao:WebGLVertexArrayObject;readonly indexCount:number;readonly indexType:number;readonly hasUvs:boolean;private buffers:WebGLBuffer[]=[];constructor(readonly gl:WebGL2RenderingContext,mesh:CpuMeshLike){const vao=gl.createVertexArray();if(!vao)throw new Error('Failed to create VAO');this.vao=vao;this.hasUvs=!!mesh.uvs;gl.bindVertexArray(vao);this.attribute(0,mesh.positions,3);this.attribute(1,mesh.normals,3);if(mesh.uvs)this.attribute(2,mesh.uvs,2);const ib=gl.createBuffer();if(!ib)throw new Error('Failed to create index buffer');this.buffers.push(ib);gl.bindBuffer(gl.ELEMENT_ARRAY_BUFFER,ib);gl.bufferData(gl.ELEMENT_ARRAY_BUFFER,mesh.indices,gl.STATIC_DRAW);this.indexCount=mesh.indices.length;this.indexType=mesh.indices instanceof Uint32Array?gl.UNSIGNED_INT:gl.UNSIGNED_SHORT;gl.bindVertexArray(null);}private attribute(location:number,data:Float32Array,size:number){const b=this.gl.createBuffer();if(!b)throw new Error('Failed to create vertex buffer');this.buffers.push(b);this.gl.bindBuffer(this.gl.ARRAY_BUFFER,b);this.gl.bufferData(this.gl.ARRAY_BUFFER,data,this.gl.STATIC_DRAW);this.gl.enableVertexAttribArray(location);this.gl.vertexAttribPointer(location,size,this.gl.FLOAT,false,0,0);}dispose():void{for(const b of this.buffers)this.gl.deleteBuffer(b);this.gl.deleteVertexArray(this.vao);this.buffers=[];}}
