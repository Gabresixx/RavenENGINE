export interface RgbaTextureSource{width:number;height:number;data:Uint8Array;}
export class GpuTexture2D{readonly texture:WebGLTexture;constructor(readonly gl:WebGL2RenderingContext,source:RgbaTextureSource,{srgb=false,mipmaps=true}:{srgb?:boolean;mipmaps?:boolean}={}){const t=gl.createTexture();if(!t)throw new Error('Failed to allocate GPU texture');this.texture=t;gl.bindTexture(gl.TEXTURE_2D,t);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_S,gl.REPEAT);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_T,gl.REPEAT);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,mipmaps?gl.LINEAR_MIPMAP_LINEAR:gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.pixelStorei(gl.UNPACK_ALIGNMENT,1);gl.texImage2D(gl.TEXTURE_2D,0,srgb?gl.SRGB8_ALPHA8:gl.RGBA8,source.width,source.height,0,gl.RGBA,gl.UNSIGNED_BYTE,source.data);if(mipmaps)gl.generateMipmap(gl.TEXTURE_2D);gl.bindTexture(gl.TEXTURE_2D,null);}dispose():void{this.gl.deleteTexture(this.texture);}}
