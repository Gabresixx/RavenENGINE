export interface RgbaTextureSource{width:number;height:number;data:Uint8Array;}
export interface GpuTexture2DOptions{srgb?:boolean;mipmaps?:boolean;wrap?:'repeat'|'clamp';anisotropy?:number;}
export class GpuTexture2D{readonly texture:WebGLTexture;constructor(readonly gl:WebGL2RenderingContext,source:RgbaTextureSource,{srgb=false,mipmaps=true,wrap='repeat',anisotropy=8}:GpuTexture2DOptions={}){const t=gl.createTexture();if(!t)throw new Error('Failed to allocate GPU texture');this.texture=t;gl.bindTexture(gl.TEXTURE_2D,t);const wrapMode=wrap==='clamp'?gl.CLAMP_TO_EDGE:gl.REPEAT;gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_S,wrapMode);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_WRAP_T,wrapMode);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MIN_FILTER,mipmaps?gl.LINEAR_MIPMAP_LINEAR:gl.LINEAR);gl.texParameteri(gl.TEXTURE_2D,gl.TEXTURE_MAG_FILTER,gl.LINEAR);gl.pixelStorei(gl.UNPACK_ALIGNMENT,1);gl.texImage2D(gl.TEXTURE_2D,0,srgb?gl.SRGB8_ALPHA8:gl.RGBA8,source.width,source.height,0,gl.RGBA,gl.UNSIGNED_BYTE,source.data);if(mipmaps)gl.generateMipmap(gl.TEXTURE_2D);const ext=gl.getExtension('EXT_texture_filter_anisotropic') as any;if(ext&&anisotropy>1){const max=gl.getParameter(ext.MAX_TEXTURE_MAX_ANISOTROPY_EXT) as number;gl.texParameterf(gl.TEXTURE_2D,ext.TEXTURE_MAX_ANISOTROPY_EXT,Math.max(1,Math.min(max,anisotropy)));}gl.bindTexture(gl.TEXTURE_2D,null);}dispose():void{this.gl.deleteTexture(this.texture);}}
