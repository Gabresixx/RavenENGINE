import{FullscreenPass}from'./FullscreenPass';const fs=`#version 300 es\nprecision highp float;in vec2 vUv;out vec4 outColor;uniform sampler2D uColor;uniform sampler2D uAo;uniform float uExposure;vec3 aces(vec3 x){const float a=2.51,b=.03,c=2.43,d=.59,e=.14;return clamp((x*(a*x+b))/(x*(c*x+d)+e),0.,1.);}void main(){vec3 c=texture(uColor,vUv).rgb;float ao=texture(uAo,vUv).r;c*=mix(1.,ao,.58);c*=exp2(uExposure);c=aces(c);c=pow(c,vec3(1./2.2));outColor=vec4(c,1);}`;export class PostProcessPass extends FullscreenPass{constructor(gl:WebGL2RenderingContext){super(gl,fs);}render(color:WebGLTexture,ao:WebGLTexture,width:number,height:number,exposure=0):void{const g=this.gl;g.bindFramebuffer(g.FRAMEBUFFER,null);g.viewport(0,0,width,height);g.disable(g.DEPTH_TEST);this.program.use();g.activeTexture(g.TEXTURE0);g.bindTexture(g.TEXTURE_2D,color);g.uniform1i(this.program.uniform('uColor'),0);g.activeTexture(g.TEXTURE1);g.bindTexture(g.TEXTURE_2D,ao);g.uniform1i(this.program.uniform('uAo'),1);g.uniform1f(this.program.uniform('uExposure'),exposure);this.draw();g.enable(g.DEPTH_TEST);}}
