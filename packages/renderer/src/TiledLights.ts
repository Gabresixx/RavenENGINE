export interface ScreenLight{x:number;y:number;radius:number;depthMin:number;depthMax:number;index:number;}export interface LightTile{indices:number[];}export interface LightTileBuild{cols:number;rows:number;tiles:LightTile[];overflow:number;}
export function buildLightTiles(width:number,height:number,tileSize:number,lights:readonly ScreenLight[],maxPerTile=32):LightTileBuild{if(width<1||height<1||tileSize<1)throw new RangeError('Invalid tiled-light dimensions');const cols=Math.ceil(width/tileSize),rows=Math.ceil(height/tileSize),tiles:LightTile[]=Array.from({length:cols*rows},():LightTile=>({indices:[]}));let overflow=0;for(const l of [...lights].sort((a,b)=>a.index-b.index)){if(l.radius<=0)continue;const minX=Math.max(0,Math.floor((l.x-l.radius)/tileSize)),maxX=Math.min(cols-1,Math.floor((l.x+l.radius)/tileSize)),minY=Math.max(0,Math.floor((l.y-l.radius)/tileSize)),maxY=Math.min(rows-1,Math.floor((l.y+l.radius)/tileSize));if(maxX<minX||maxY<minY)continue;for(let y=minY;y<=maxY;y++)for(let x=minX;x<=maxX;x++){const t=tiles[y*cols+x];if(t.indices.length<maxPerTile)t.indices.push(l.index);else overflow++;}}return{cols,rows,tiles,overflow};}
