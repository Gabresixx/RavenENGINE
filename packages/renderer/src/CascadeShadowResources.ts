export class CascadeShadowResources{texture!:WebGLTexture;framebuffer!:WebGLFramebuffer;size=0;layers=0;constructor(readonly gl:WebGL2RenderingContext){}ensure(size:number,layers=3):void{if(this.size===size&&this.layers===layers)return;this.dispose();const g=this.gl,t=g.createTexture(),f=g.createFramebuffer();if(!t||!f)throw new Error('Failed to allocate cascade shadows');this.texture=t;this.framebuffer=f;this.size=size;this.layers=layers;g.bindTexture(g.TEXTURE_2D_ARRAY,t);g.texParameteri(g.TEXTURE_2D_ARRAY,g.TEXTURE_MIN_FILTER,g.LINEAR);g.texParameteri(g.TEXTURE_2D_ARRAY,g.TEXTURE_MAG_FILTER,g.LINEAR);g.texParameteri(g.TEXTURE_2D_ARRAY,g.TEXTURE_WRAP_S,g.CLAMP_TO_EDGE);g.texParameteri(g.TEXTURE_2D_ARRAY,g.TEXTURE_WRAP_T,g.CLAMP_TO_EDGE);g.texImage3D(g.TEXTURE_2D_ARRAY,0,g.DEPTH_COMPONENT24,size,size,layers,0,g.DEPTH_COMPONENT,g.UNSIGNED_INT,null);g.bindFramebuffer(g.FRAMEBUFFER,f);g.drawBuffers([g.NONE]);g.readBuffer(g.NONE);g.bindFramebuffer(g.FRAMEBUFFER,null);}bindLayer(layer:number):void{if(layer<0||layer>=this.layers)throw new RangeError('Invalid cascade layer');this.gl.bindFramebuffer(this.gl.FRAMEBUFFER,this.framebuffer);this.gl.framebufferTextureLayer(this.gl.FRAMEBUFFER,this.gl.DEPTH_ATTACHMENT,this.texture,0,layer);this.gl.viewport(0,0,this.size,this.size);}dispose():void{if(this.texture)this.gl.deleteTexture(this.texture);if(this.framebuffer)this.gl.deleteFramebuffer(this.framebuffer);this.size=this.layers=0;}}
