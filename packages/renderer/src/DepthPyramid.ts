export interface DepthMip{width:number;height:number;data:Float32Array;}
export function buildCpuDepthPyramid(base:DepthMip):DepthMip[]{const levels=[base];let prev=base;while(prev.width>1||prev.height>1){const w=Math.max(1,Math.ceil(prev.width/2)),h=Math.max(1,Math.ceil(prev.height/2)),data=new Float32Array(w*h);for(let y=0;y<h;y++)for(let x=0;x<w;x++){let m=1;for(let oy=0;oy<2;oy++)for(let ox=0;ox<2;ox++){const sx=Math.min(prev.width-1,x*2+ox),sy=Math.min(prev.height-1,y*2+oy);m=Math.min(m,prev.data[sy*prev.width+sx]);}data[y*w+x]=m;}prev={width:w,height:h,data};levels.push(prev);}return levels;}
