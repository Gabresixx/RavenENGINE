export class SceneTargets{
  color!:WebGLTexture;depth!:WebGLTexture;framebuffer!:WebGLFramebuffer;width=0;height=0;
  constructor(readonly gl:WebGL2RenderingContext){}
  ensure(width:number,height:number):void{if(this.width===width&&this.height===height)return;this.dispose();this.width=width;this.height=height;const g=this.gl,c=g.createTexture(),d=g.createTexture(),f=g.createFramebuffer();if(!c||!d||!f)throw new Error('Failed to allocate RAVEN scene targets');this.color=c;this.depth=d;this.framebuffer=f;g.bindTexture(g.TEXTURE_2D,c);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_MIN_FILTER,g.LINEAR);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_MAG_FILTER,g.LINEAR);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_WRAP_S,g.CLAMP_TO_EDGE);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_WRAP_T,g.CLAMP_TO_EDGE);g.texImage2D(g.TEXTURE_2D,0,g.RGBA8,width,height,0,g.RGBA,g.UNSIGNED_BYTE,null);g.bindTexture(g.TEXTURE_2D,d);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_MIN_FILTER,g.NEAREST);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_MAG_FILTER,g.NEAREST);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_WRAP_S,g.CLAMP_TO_EDGE);g.texParameteri(g.TEXTURE_2D,g.TEXTURE_WRAP_T,g.CLAMP_TO_EDGE);g.texImage2D(g.TEXTURE_2D,0,g.DEPTH_COMPONENT24,width,height,0,g.DEPTH_COMPONENT,g.UNSIGNED_INT,null);g.bindFramebuffer(g.FRAMEBUFFER,f);g.framebufferTexture2D(g.FRAMEBUFFER,g.COLOR_ATTACHMENT0,g.TEXTURE_2D,c,0);g.framebufferTexture2D(g.FRAMEBUFFER,g.DEPTH_ATTACHMENT,g.TEXTURE_2D,d,0);const status=g.checkFramebufferStatus(g.FRAMEBUFFER);g.bindFramebuffer(g.FRAMEBUFFER,null);if(status!==g.FRAMEBUFFER_COMPLETE){this.dispose();throw new Error(`Incomplete scene framebuffer: 0x${status.toString(16)}`);}}
  dispose():void{if(this.color)this.gl.deleteTexture(this.color);if(this.depth)this.gl.deleteTexture(this.depth);if(this.framebuffer)this.gl.deleteFramebuffer(this.framebuffer);this.width=this.height=0;}
}
