export type FaultSeverity='info'|'warn'|'error'|'fatal';
export class EngineFault extends Error{constructor(readonly subsystem:string,readonly code:string,message:string,readonly severity:FaultSeverity='error',readonly recoverable=true,options?:ErrorOptions){super(message,options);this.name='EngineFault';}}
export class FaultBoundary{private listeners=new Set<(fault:EngineFault)=>void>();onFault(listener:(fault:EngineFault)=>void):()=>void{this.listeners.add(listener);return()=>this.listeners.delete(listener);}capture(error:unknown,subsystem='unknown',code='unhandled'):EngineFault{const fault=error instanceof EngineFault?error:new EngineFault(subsystem,code,error instanceof Error?error.message:String(error),'error',true,{cause:error});for(const listener of this.listeners)listener(fault);return fault;}}
