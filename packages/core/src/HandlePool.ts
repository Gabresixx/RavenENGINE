export interface Handle{index:number;generation:number;}
export class HandlePool<T>{private values:(T|undefined)[]=[];private generations:number[]=[];private free:number[]=[];create(value:T):Handle{const index=this.free.pop()??this.values.length;if(index===this.values.length){this.values.push(value);this.generations.push(0);}else this.values[index]=value;return{index,generation:this.generations[index]};}get(handle:Handle):T|undefined{return this.generations[handle.index]===handle.generation?this.values[handle.index]:undefined;}require(handle:Handle):T{const v=this.get(handle);if(v===undefined)throw new Error(`Stale handle ${handle.index}:${handle.generation}`);return v;}destroy(handle:Handle):boolean{if(this.get(handle)===undefined)return false;this.values[handle.index]=undefined;this.generations[handle.index]++;this.free.push(handle.index);return true;}*entries():IterableIterator<[Handle,T]>{for(let index=0;index<this.values.length;index++){const value=this.values[index];if(value!==undefined)yield[{index,generation:this.generations[index]},value];}}get size():number{return this.values.length-this.free.length;}}
