export class BoundedQueue<T>{private data:T[]=[];constructor(readonly capacity:number){if(capacity<1)throw new RangeError('capacity must be positive');}push(value:T):boolean{if(this.data.length>=this.capacity)return false;this.data.push(value);return true;}shift():T|undefined{return this.data.shift();}get size():number{return this.data.length;}get full():boolean{return this.data.length>=this.capacity;}clear():void{this.data.length=0;}}
