import type { EngineModule } from './Lifecycle';
export class ModuleGraph{private readonly modules=new Map<string,EngineModule>();register<T extends EngineModule>(module:T):T{if(this.modules.has(module.id))throw new Error(`Duplicate module ${module.id}`);this.modules.set(module.id,module);return module;}get(id:string):EngineModule|undefined{return this.modules.get(id);}ordered():EngineModule[]{const out:EngineModule[]=[],temporary=new Set<string>(),permanent=new Set<string>();const visit=(id:string)=>{if(permanent.has(id))return;if(temporary.has(id))throw new Error(`Module dependency cycle at ${id}`);const m=this.modules.get(id);if(!m)throw new Error(`Missing module dependency ${id}`);temporary.add(id);for(const dep of m.dependencies??[])visit(dep);temporary.delete(id);permanent.add(id);out.push(m);};for(const id of [...this.modules.keys()].sort())visit(id);return out;}clear():void{this.modules.clear();}}
