export interface QualityState{renderScale:number;shadowScale:number;aoScale:number;volumetricScale:number;reflectionScale:number;particleScale:number;geometryBudget:number;animationBudget:number;}
export class QualityGovernor{readonly state:QualityState={renderScale:1,shadowScale:1,aoScale:.5,volumetricScale:.5,reflectionScale:.5,particleScale:1,geometryBudget:1,animationBudget:1};constructor(readonly targetFrameMs=16.6667){}update(frameMs:number,cpuSimulationMs:number):QualityState{const overload=frameMs/this.targetFrameMs;if(overload>1.08){this.state.reflectionScale=Math.max(.25,this.state.reflectionScale-.05);this.state.volumetricScale=Math.max(.25,this.state.volumetricScale-.05);this.state.aoScale=Math.max(.25,this.state.aoScale-.04);this.state.shadowScale=Math.max(.5,this.state.shadowScale-.05);this.state.renderScale=Math.max(.67,this.state.renderScale-.02);this.state.particleScale=Math.max(.45,this.state.particleScale-.03);}else if(overload<.78){this.state.renderScale=Math.min(1,this.state.renderScale+.01);this.state.shadowScale=Math.min(1,this.state.shadowScale+.02);this.state.aoScale=Math.min(.75,this.state.aoScale+.015);this.state.volumetricScale=Math.min(1,this.state.volumetricScale+.02);this.state.reflectionScale=Math.min(1,this.state.reflectionScale+.02);this.state.particleScale=Math.min(1,this.state.particleScale+.02);}if(cpuSimulationMs>this.targetFrameMs*.45){this.state.animationBudget=Math.max(.4,this.state.animationBudget-.05);this.state.geometryBudget=Math.max(.55,this.state.geometryBudget-.02);}else{this.state.animationBudget=Math.min(1,this.state.animationBudget+.01);this.state.geometryBudget=Math.min(1,this.state.geometryBudget+.005);}return this.state;}}
