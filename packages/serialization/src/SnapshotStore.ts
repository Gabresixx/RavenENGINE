import type { KeyValueStore } from '@raven/platform';import type { BinaryCodec } from './Codec';import { decodeSnapshot,encodeSnapshot } from './Snapshot';
export class SnapshotStore{constructor(readonly storage:KeyValueStore,readonly engineAbi:number,readonly prefix='snapshot'){}private key(slot:string):string{return `${this.prefix}:${slot}`;}async save<T>(slot:string,codec:BinaryCodec<T>,value:T):Promise<void>{await this.storage.set(this.key(slot),encodeSnapshot(codec,value,this.engineAbi));}async load<T>(slot:string,codec:BinaryCodec<T>):Promise<T|undefined>{const bytes=await this.storage.get(this.key(slot));return bytes?decodeSnapshot(bytes,codec,this.engineAbi).value:undefined;}async delete(slot:string):Promise<void>{await this.storage.delete(this.key(slot));}}
