export class RavenAudio{private ctx?:AudioContext;private master?:GainNode;async resume():Promise<void>{if(!this.ctx){this.ctx=new AudioContext();this.master=this.ctx.createGain();this.master.gain.value=.8;this.master.connect(this.ctx.destination);}if(this.ctx.state!=='running')await this.ctx.resume();}get context():AudioContext{if(!this.ctx)throw new Error('Audio not initialized; call resume() from a user gesture');return this.ctx;}get output():AudioNode{return this.master??this.context.destination;}setMasterGain(v:number):void{if(this.master)this.master.gain.setTargetAtTime(Math.max(0,v),this.context.currentTime,.02);}createPanner(position:[number,number,number],maxDistance=30):PannerNode{const p=this.context.createPanner();p.panningModel='HRTF';p.distanceModel='inverse';p.maxDistance=maxDistance;p.positionX.value=position[0];p.positionY.value=position[1];p.positionZ.value=position[2];p.connect(this.output);return p;}}
