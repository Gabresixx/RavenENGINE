import type{Vec3}from'./Vec3';export type Quat=[number,number,number,number];export const quatIdentity=():Quat=>[0,0,0,1];export function quatNormalize(q:Quat):Quat{const l=Math.max(1e-8,Math.hypot(...q));return[q[0]/l,q[1]/l,q[2]/l,q[3]/l];}export function quatMul(a:Quat,b:Quat):Quat{const[ax,ay,az,aw]=a,[bx,by,bz,bw]=b;return[aw*bx+ax*bw+ay*bz-az*by,aw*by-ax*bz+ay*bw+az*bx,aw*bz+ax*by-ay*bx+az*bw,aw*bw-ax*bx-ay*by-az*bz];}export function quatFromAxisAngle(axis:Vec3,angle:number):Quat{const h=angle*.5,s=Math.sin(h);return quatNormalize([axis[0]*s,axis[1]*s,axis[2]*s,Math.cos(h)]);}
