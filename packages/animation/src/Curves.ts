export const saturate=(x:number)=>Math.max(0,Math.min(1,x));export const smooth01=(x:number)=>{x=saturate(x);return x*x*(3-2*x);};export const smoother01=(x:number)=>{x=saturate(x);return x*x*x*(x*(x*6-15)+10);};export const parabola01=(x:number)=>{x=saturate(x);return 4*x*(1-x);};export const remap01=(x:number,a:number,b:number)=>saturate((x-a)/Math.max(1e-8,b-a));export function pulse(x:number,start:number,peak:number,end:number):number{if(x<=start||x>=end)return 0;return x<peak?smooth01(remap01(x,start,peak)):1-smooth01(remap01(x,peak,end));}
