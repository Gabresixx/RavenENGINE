import type{MotionIntent}from'./MotionIntent';import{ProceduralMotionField,type MotionPhase}from'./MotionField';import{predictTrajectory,type TrajectoryState,type TrajectoryPoint}from'./TrajectoryPredictor';
export interface PlannedBodyMotion{motion:MotionPhase;trajectory:TrajectoryPoint[];desiredSpeed:number;turnUrgency:number;}
export class FullBodyMotionPlanner{readonly field=new ProceduralMotionField();plan(intent:MotionIntent,state:TrajectoryState,dt:number):PlannedBodyMotion{const trajectory=predictTrajectory(state,intent.desiredVelocity),motion=this.field.sample(intent,dt),desiredSpeed=Math.hypot(intent.desiredVelocity[0],intent.desiredVelocity[2]),currentFacing=Math.atan2(state.velocity[0],-state.velocity[2]),desiredFacing=Math.atan2(intent.desiredFacing[0],-intent.desiredFacing[2]);let delta=desiredFacing-currentFacing;while(delta>Math.PI)delta-=Math.PI*2;while(delta<-Math.PI)delta+=Math.PI*2;return{motion,trajectory,desiredSpeed,turnUrgency:Math.min(1,Math.abs(delta)/Math.PI)};}}
