import{FixedStepClock}from'@raven/core';import{ProductionSceneRenderer,type ProductionRenderStats}from'@raven/renderer';import{extractPbrScene,type RavenScene}from'./Scene';import{TransformSystem}from'./TransformSystem';
export interface RuntimePipelineOptions{simulationHz?:number;onFixedStep?:(dt:number)=>void;aoScale?:number;exposure?:number;}
export class RuntimePipeline{readonly transforms=new TransformSystem();readonly sceneRenderer:ProductionSceneRenderer;readonly fixed:FixedStepClock;constructor(readonly scene:RavenScene,canvas:HTMLCanvasElement,readonly options:RuntimePipelineOptions={}){const gl=canvas.getContext('webgl2',{alpha:false,antialias:false,depth:true,stencil:false,powerPreference:'high-performance'});if(!gl)throw new Error('RAVEN requires WebGL2');this.sceneRenderer=new ProductionSceneRenderer(gl,canvas);this.fixed=new FixedStepClock(1000/(options.simulationHz??60));}frame(cameraEntity:number,deltaMs:number,cssWidth:number,cssHeight:number,dpr:number):ProductionRenderStats{this.fixed.advance(deltaMs,dt=>this.options.onFixedStep?.(dt));this.transforms.update(this.scene);const extracted=extractPbrScene(this.scene,cameraEntity);return this.sceneRenderer.render({camera:extracted.camera,draws:extracted.draws,cssWidth,cssHeight,devicePixelRatio:dpr,frameMs:deltaMs,aoScale:this.options.aoScale,exposure:this.options.exposure});}dispose():void{this.sceneRenderer.dispose();}}
