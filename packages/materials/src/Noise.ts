const fract=(x:number)=>x-Math.floor(x);const fade=(t:number)=>t*t*(3-2*t);export function hash2(x:number,y:number,seed=0):number{let h=(Math.imul(x|0,374761393)^Math.imul(y|0,668265263)^Math.imul(seed|0,1274126177))>>>0;h=Math.imul(h^(h>>>13),1274126177);return((h^(h>>>16))>>>0)/4294967296;}export function valueNoise2(x:number,y:number,seed=0):number{const ix=Math.floor(x),iy=Math.floor(y),fx=fade(fract(x)),fy=fade(fract(y)),a=hash2(ix,iy,seed),b=hash2(ix+1,iy,seed),c=hash2(ix,iy+1,seed),d=hash2(ix+1,iy+1,seed),u=a+(b-a)*fx,v=c+(d-c)*fx;return u+(v-u)*fy;}export function fbm2(x:number,y:number,seed=0,octaves=5,lacunarity=2,gain=.5):number{let sum=0,amp=.5,freq=1,norm=0;for(let i=0;i<octaves;i++){sum+=valueNoise2(x*freq,y*freq,seed+i*1013)*amp;norm+=amp;freq*=lacunarity;amp*=gain;}return sum/Math.max(1e-8,norm);}export function ridge2(x:number,y:number,seed=0,octaves=4):number{return 1-Math.abs(fbm2(x,y,seed,octaves)*2-1);}
