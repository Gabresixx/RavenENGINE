export interface Particle{position:[number,number,number];velocity:[number,number,number];age:number;lifetime:number;size:number;kind:number;}
export class ParticlePool{readonly data:Particle[]=[];constructor(readonly capacity=20000){}emit(p:Particle):void{if(this.data.length>=this.capacity)this.data.shift();this.data.push(p);}step(dt:number,wind:[number,number,number]=[0,0,0]):void{for(let i=this.data.length-1;i>=0;i--){const p=this.data[i];p.age+=dt;if(p.age>=p.lifetime){this.data.splice(i,1);continue;}p.velocity[0]+=wind[0]*dt*.15;p.velocity[1]+=(wind[1]-2.8)*dt;p.velocity[2]+=wind[2]*dt*.15;p.position[0]+=p.velocity[0]*dt;p.position[1]+=p.velocity[1]*dt;p.position[2]+=p.velocity[2]*dt;}}}
