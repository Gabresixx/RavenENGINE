export interface WindObstacle{x:number;z:number;radius:number;strength:number;}
export class UnifiedWindField{direction:[number,number,number]=[1,0,.25];speed=2;gust=.35;readonly obstacles:WindObstacle[]=[];sample(x:number,y:number,z:number,time:number):[number,number,number]{const g=1+Math.sin(time*.71+x*.027+z*.019)*this.gust;let vx=this.direction[0]*this.speed*g,vy=this.direction[1]*this.speed*g,vz=this.direction[2]*this.speed*g;for(const o of this.obstacles){const dx=x-o.x,dz=z-o.z,d=Math.hypot(dx,dz);if(d<o.radius&&d>1e-4){const f=(1-d/o.radius)*o.strength;vx+=-dz/d*f;vz+=dx/d*f;}}return[vx,vy,vz];}}
